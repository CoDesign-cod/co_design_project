import 'package:flutter/material.dart';

class navBarProfilePage extends StatelessWidget {
  const navBarProfilePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
